# Returns the `NAflag` for a `SpatRaster`, making one up if it's not defined

Returns the `NAflag` for a `SpatRaster`, making one up if it's not
defined

## Usage

``` r
get_NAflag(rast)
```

## Arguments

- rast:

  A `SpatRaster` object

## Value

The existing or assigned `NAflag`
