# Initialize user parameters when the package is loaded

Initialize user parameters when the package is loaded

## Usage

``` r
.onLoad(libname, pkgname)
```

## Arguments

- libname:

  (required by R; not used)

- pkgname:

  (required by R; not used)
