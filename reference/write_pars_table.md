# Write a parameter table

Given a parameter name the corresponds to tab-delimited file, write a
data frame to it.

## Usage

``` r
write_pars_table(data, name)
```

## Arguments

- data:

  Data frame to write

- name:

  Parameter name
