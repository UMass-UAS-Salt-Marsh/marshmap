# Produce a zoomed crop into the center of a `terra` object

Produce a zoomed crop into the center of a `terra` object

## Usage

``` r
center_zoom(x, factor)
```

## Arguments

- x:

  A `terra` object

- factor:

  Proportion of image to include
