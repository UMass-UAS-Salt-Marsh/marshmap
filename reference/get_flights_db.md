# Get the flights database for a site

Get the flights database for a site

## Usage

``` r
get_flights_db(site, noerror = FALSE)
```

## Arguments

- site:

  A single site name (3 letter code), already vetted

- noerror:

  If TRUE, return NULL instead of error

## Value

the flights database
