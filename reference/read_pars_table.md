# Read a parameter table

Given a parameter name the corresponds to tab-delimited file, read and
return the file

## Usage

``` r
read_pars_table(name)
```

## Arguments

- name:

  Parameter name

## Value

Data frame of the parameter file
