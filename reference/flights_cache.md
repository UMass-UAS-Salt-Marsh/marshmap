# Create missing our outdated cached images for screen()

Create missing our outdated cached images for screen()

## Usage

``` r
flights_cache(site, replace_caches = FALSE)
```

## Arguments

- site:

  site, using 3 letter abbreviation

- replace_caches:

  If TRUE, all cached images (used for `screen`) are replaced
