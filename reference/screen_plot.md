# Display plots for screen

Display plots for screen

## Usage

``` r
screen_plot(extent, input, output, session)
```

## Arguments

- extent:

  Which plot to show, one of `full`, `inset1`, or `inset2`

- input:

  Shiny input object

- output:

  Shiny output object

- session:

  Shiny session object
