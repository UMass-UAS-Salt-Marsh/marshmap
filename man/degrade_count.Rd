% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/degrade_count.R
\name{degrade_count}
\alias{degrade_count}
\title{Run the U-Net site-count degradation experiment}
\usage{
degrade_count(
  model = "primary_v6",
  train = "train",
  exp = "degrade",
  counts = NULL,
  seeds = NULL,
  radius = NULL,
  resources = NULL,
  local = FALSE,
  trap = TRUE,
  requirecuda = TRUE,
  save_gis = FALSE,
  folds = NULL,
  anchor = NULL,
  pin_weights = NULL,
  comment = NULL
)
}
\arguments{
\item{model}{Base name of the model \code{.yml} in \verb{<pars>/unet/} (e.g. \code{'primary_v6'}).}

\item{train}{Base name of the training \code{.yml} in \verb{<pars>/unet/}, or NULL.}

\item{exp}{Base name of the experiment \code{.yml} in \verb{<pars>/unet/} supplying \code{folds}
(or \code{test_group}/\code{val_group}) and \code{seeds}, and optionally \code{counts},
\code{count_radius}, \code{count_anchor}, \code{pin_class_weights}.}

\item{counts}{Optional integer vector of training-site counts to sweep; overrides
\code{config$counts} (default \code{c(4, 8, 16, 32, 64)}).}

\item{seeds}{Optional integer vector of seeds (vary network init, data order, AND
which sites are drawn); overrides the experiment YAML.}

\item{radius}{Fixed plot radius (m) applied to every cell; overrides
\code{config$count_radius} (default \code{0.5}).}

\item{resources}{Slurm launch resources; see \link[slurmcollie]{launch}. Take
priority over the GPU defaults.}

\item{local}{If TRUE, run locally; otherwise spawn batch jobs on Unity.}

\item{trap}{If TRUE, trap errors in local mode (see \code{\link[=train]{train()}}).}

\item{requirecuda}{If TRUE (default), abort if CUDA is unavailable.}

\item{save_gis}{If TRUE, write plot centers, carved disks, and sampled training
polys as GeoPackages under each cell's \verb{gis/} for inspection in QGIS.}

\item{folds}{Optional fold spec overriding \code{config$folds}: a list of \code{c(test, val)}
group pairs (or a data.frame with \code{test}/\code{val} columns).}

\item{anchor}{If TRUE, also run the all-sites endpoint (\code{count = Inf}) as the
right-hand anchor of the curve. Defaults to \code{config$count_anchor}, else TRUE.}

\item{pin_weights}{If TRUE (default via \code{config$pin_class_weights}), train every
cell with class weights pinned to the fold's full-transect frequency, so the loss
objective does not shift as sites are dropped.}

\item{comment}{Optional slurmcollie comment.}
}
\description{
Launches the site-count degradation experiment, which measures how U-Net accuracy
responds to the NUMBER of training sites (transects) at a fixed plot size. For each
spatial fold and site count, a random subset of the fold's training transects is
drawn (resampled per seed), synthetic plots are placed on their centerlines and
carved to a fixed-radius disk, and the model is trained and evaluated on the fold's
full-extent test set. Single-stage: one GPU job per fold x count x seed cell.
Companion to \code{\link[=degrade]{degrade()}} (the plot-size sweep); see \code{degrade_count.R}.
}
