% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/degrade_count.R
\name{summarize_degrade_count}
\alias{summarize_degrade_count}
\title{Summarize a site-count degradation experiment}
\usage{
summarize_degrade_count(
  exp = "degrade",
  model = "primary_v6",
  results = NULL,
  train = "train",
  error = c("se", "sd", "none")
)
}
\arguments{
\item{exp}{Experiment YAML base name (to locate the results directory).}

\item{model}{Model YAML base name (to locate the results directory).}

\item{results}{Optional data.frame of result rows; if NULL, reads \code{countcell_*.csv}.}

\item{train}{Training YAML base name (used only to resolve the site/config).}

\item{error}{Error-bar type drawn on each series: \code{'se'} (standard error over
folds x seeds, default), \code{'sd'}, or \code{'none'}.}
}
\value{
Invisibly, the aggregated data.frame (per-count means, \verb{_sd}, \verb{_se}, \code{n}).
}
\description{
Reads the per-cell rows written by \code{\link[=do_degrade_count]{do_degrade_count()}}, averages over folds and
seeds by requested site count, prints a compact table, and plots overall CCR (and
per-class recall) versus the mean number of training sites on a log x-axis. The
all-sites anchor (\code{count = Inf}) is drawn as the right-most point and marked with a
vertical guide.
}
