% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_classified_tif.R
\name{write_classified_tif}
\alias{write_classified_tif}
\title{Write a categorical raster to GeoTIFF with color table and VAT}
\usage{
write_classified_tif(
  r,
  destination,
  class_lookup,
  overwrite = FALSE,
  qml = FALSE
)
}
\arguments{
\item{r}{(SpatRaster) categorical raster, integer-valued, with values
in 0-255 (INT1U range). NA-valued cells are written as NoData.}

\item{destination}{(character) path to the output GeoTIFF.}

\item{class_lookup}{(data frame) with columns \code{value}, \code{name}, \code{color},
one row per class code present in \code{r}. Typically built with
\code{build_class_lookup()}.}

\item{overwrite}{(logical) if \code{TRUE}, an existing destination is replaced.}

\item{qml}{(logical) if \code{TRUE}, also write a \code{.qml} sidecar with sparse
symbology for QGIS. Not yet implemented; included as a stub for future
use.}
}
\value{
Invisibly returns \code{destination}.
}
\description{
Takes an in-memory categorical SpatRaster and a class lookup table and
writes a final, GIS-ready GeoTIFF: LZW-compressed, tiled, with a
GDAL color table (so values display correctly in QGIS/terra without
extra setup) and an ESRI Value Attribute Table sidecar (so values
display correctly in ArcGIS).
}
\details{
This consolidates the standard \code{addColorTable} + \code{makeNiceTif} + \code{addVat}
dance used in \code{do_map} and \code{unet_assemble_map}. Note that GDAL color tables
are dense over 0-255: ArcGIS will show every unused integer in the legend
until you switch the symbology to "Unique Values". A future enhancement
may write a \code{.qml} (QGIS) or \code{.lyrx} (ArcGIS) sidecar to clean this up.
}
