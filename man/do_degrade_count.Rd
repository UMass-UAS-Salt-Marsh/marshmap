% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/do_degrade_count.R
\name{do_degrade_count}
\alias{do_degrade_count}
\title{Subsample sites, train, and evaluate one cell of the site-count experiment}
\usage{
do_degrade_count(
  rep,
  grid,
  exp,
  model,
  train,
  radius = 0.5,
  requirecuda = TRUE,
  pin_weights = FALSE,
  save_gis = FALSE
)
}
\arguments{
\item{rep}{Row index into \code{grid} (supplied by slurmcollie).}

\item{grid}{data.frame with \code{count}, \code{seed}, \code{test}, \code{val} columns (fold x count x seed).}

\item{exp}{Experiment YAML base name in \verb{<pars>/unet/}.}

\item{model}{Model YAML base name in \verb{<pars>/unet/}.}

\item{train}{Training YAML base name in \verb{<pars>/unet/}, or NULL.}

\item{radius}{Fixed plot radius (m) applied to every cell.}

\item{requirecuda}{If TRUE (default), abort if CUDA is unavailable.}

\item{pin_weights}{If TRUE, train with class weights pinned to the fold's
full-transect frequency (read/computed from \verb{f<test>/class_weights_pinned.json}).}

\item{save_gis}{If TRUE, save plot centers, carved disks, and sampled training polys
as GeoPackages under the cell's \verb{gis/} for inspection.}
}
\description{
Per-cell worker for \code{\link[=degrade_count]{degrade_count()}}. For one spatial fold (a test/val group pair),
one requested site count, and one seed, it builds the input stack and prepared
transects via \code{\link[=unet_prep_setup]{unet_prep_setup()}}, makes the fold's fixed 3-way spatial split, draws
a random subset of the fold's TRAINING transects (resampled per seed, nested within
a seed), places synthetic plots on those transects' centerlines, carves them to the
fixed radius, extracts + exports patches, trains the U-Net, evaluates on the fold's
full-extent test set, and writes one tidy result row. A count of \code{Inf} is the
all-sites anchor (every training transect). Single-stage: because the subset depends
on the seed, carving is seed-specific and cannot be shared across cells.
}
\details{
Reads/writes under \verb{<unetdir>/<model>/degrade/f<test>/count/c<NNN|all>/s<seed>/}
(\verb{patches/set1/} for exports, \verb{fit/set1/} for the model) and appends a per-cell row
\verb{.../degrade/countcell_f<test>_c<NNN>_s<seed>.csv}. Each cell writes its own file to
avoid concurrent-write races across the Slurm array. The fold's pinned class weights
(\verb{f<test>/class_weights_pinned.json}, from the full training transects) are reused if
present and computed atomically if not.
}
