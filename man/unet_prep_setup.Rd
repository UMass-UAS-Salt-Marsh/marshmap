% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unet_prep_setup.R
\name{unet_prep_setup}
\alias{unet_prep_setup}
\title{Build the U-Net input stack and prepared transects}
\usage{
unet_prep_setup(config)
}
\arguments{
\item{config}{A config list already passed through \code{\link[=unet_config_defaults]{unet_config_defaults()}}.}
}
\value{
List with \code{input_stack} (SpatRaster) and \code{transects} (sf, post
\code{spatial_holdout}, smoothed/upscaled to match the stack).
}
\description{
Shared setup for \code{do_unet_prep} and the pixel-degradation experiment
(\code{do_degrade_prep}). Builds the multi-band input stack, reads and cleans the
field transects (reclass, class/year filtering, overlap removal, geometry
repair), assigns spatially-distributed holdout groups (\code{bypoly00}), and applies
optional smoothing and upscaling. Keeping this in one place guarantees the
experiment's train/val/test split is byte-for-byte identical to the production
pipeline's.
}
\keyword{internal}
