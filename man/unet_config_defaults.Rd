% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unet_config_defaults.R
\name{unet_config_defaults}
\alias{unet_config_defaults}
\title{Normalize and derive a U-Net model config}
\usage{
unet_config_defaults(config)
}
\arguments{
\item{config}{Config list read from a model \code{.yml}.}
}
\value{
The enriched config list.
}
\description{
Fills in defaults and derives helper fields for a U-Net model config (as read
from a \verb{<model>.yml} in \verb{<pars>/unet/}). Shared by \code{do_unet_prep} and the
pixel-degradation experiment (\code{do_degrade_prep}) so both see an identical,
fully-populated config.
}
\details{
Derives \code{fpath}, \code{bands}, \code{n_channels}, per-ortho \code{type}, \code{class_mapping}, and
\code{seed}; supplies defaults for \code{transects}, \code{reclass}, \code{upscale}, \code{smooth},
\code{holdout_col}, \code{cv}, \code{val}, and \code{test}; and validates the cross-validation grid.
}
\keyword{internal}
