% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/do_layer.R
\name{do_layer}
\alias{do_layer}
\title{Combine multiple geoTIFF maps into a single integrated map}
\usage{
do_layer(name, rep = NULL)
}
\arguments{
\item{name}{(character) base name of the YAML config in \verb{pars/unet/}.
For example, \code{'test1'} reads \code{pars/unet/test1.yml}.}

\item{rep}{Throwaway argument to make \code{slurmcollie} happy.}
}
\value{
Invisibly returns the path to the integrated GeoTIFF.
}
\description{
Worker function for \code{layer()}. Reads a base map and recursively fills
specified codes with values from other maps, producing an integrated
classification raster. Each fill rule replaces all cells with a given
code in its parent map with the corresponding cells from a fill map.
Fill rules can nest arbitrarily: a code in a fill map can itself be
filled from yet another map.
}
\details{
Produces:
\enumerate{
\item The integrated map as \verb{<result>.tif} (LZW-compressed, with color
table and VAT)
\item A YAML sidecar \verb{<result>_layer.yml} documenting the resolved fill
tree, the final class legend (code, name, source map), and a
timestamp
}
}
