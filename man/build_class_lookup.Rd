% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_class_lookup.R
\name{build_class_lookup}
\alias{build_class_lookup}
\title{Build a class lookup table by searching across class-code columns}
\usage{
build_class_lookup(codes, classes_path, levels = NULL, allowmissing = FALSE)
}
\arguments{
\item{codes}{(integer) vector of class codes appearing in the map.
Must be in 0-255 (INT1U range).}

\item{classes_path}{(character) path to \code{classes.txt} (tab-separated).
Defaults to the path returned by \code{read_pars_table('classes')} when
called via the standard project workflow; for standalone use, supply
explicitly.}

\item{levels}{(character) optional vector of column names to restrict
the search to (e.g., \code{c('subclass', 'ICS_V4')}). If \code{NULL} (default),
all class-code columns are searched.}

\item{allowmissing}{(logical) if \code{FALSE} (default), missing codes throw
an error. If \code{TRUE}, missing codes generate a warning and are dropped
from the lookup.}
}
\value{
A data frame with columns \code{value} (integer), \code{name} (character),
and \code{color} (character, hex like \code{'#27408b'}), one row per code, sorted
by \code{value}.
}
\description{
Searches for each given class code across all class-code columns in
\code{classes.txt} (\code{subclass}, \code{ICS_V4}, \code{ICS_V5}, \code{ICS_V6}) and returns a
data frame of \code{value}, \code{name}, and \code{color}. This supports integrated
maps that mix levels (e.g., \code{subclass}-level water codes 21/22 alongside
\code{ICS_V4}-level vegetation codes 102/104).
}
\details{
For each code, all matching \verb{(name, color)} pairs across columns are
collected. If a unique pair is found, it's used. If multiple distinct
pairs are found, the function errors with the conflicting columns named.
If no match is found, the function errors unless \code{allowmissing = TRUE},
in which case the code is dropped from the lookup with a warning.

Rows where the code is 999 or the name is \code{'xxx'} are treated as
placeholders and ignored during the search.
}
