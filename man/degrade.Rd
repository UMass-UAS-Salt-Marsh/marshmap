% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/degrade.R
\name{degrade}
\alias{degrade}
\title{Run the U-Net pixel-degradation experiment}
\usage{
degrade(
  model = "primary_v6",
  train = "train",
  exp = "degrade",
  stage = c("prep", "train"),
  radii = NULL,
  seeds = NULL,
  resources = NULL,
  local = FALSE,
  trap = TRUE,
  requirecuda = TRUE,
  save_gis = FALSE,
  folds = NULL,
  anchor = NULL,
  pin_weights = NULL,
  comment = NULL
)
}
\arguments{
\item{model}{Base name of the model \code{.yml} in \verb{<pars>/unet/} (e.g. \code{'primary_v6'}).}

\item{train}{Base name of the training \code{.yml} in \verb{<pars>/unet/}, or NULL.}

\item{exp}{Base name of the experiment \code{.yml} in \verb{<pars>/unet/} supplying
\code{radii}, \code{seeds}, \code{spacing_m}, \code{test_group}, \code{val_group}.}

\item{stage}{\code{'prep'} (carve + export patches, one job per radius) or \code{'train'}
(train + evaluate, one job per radius x seed cell).}

\item{radii}{Optional numeric vector of plot radii (m); overrides the experiment YAML.}

\item{seeds}{Optional integer vector of training seeds; overrides the experiment YAML.}

\item{resources}{Slurm launch resources; see \link[slurmcollie]{launch}. Take
priority over the stage defaults. \code{train} requires a GPU.}

\item{local}{If TRUE, run locally; otherwise spawn batch jobs on Unity.}

\item{trap}{If TRUE, trap errors in local mode (see \code{\link[=train]{train()}}).}

\item{requirecuda}{If TRUE (default), abort if CUDA is unavailable (train stage).}

\item{save_gis}{If TRUE (prep stage only), write plot centers, clipped disks, and
training polys as GeoPackages under \verb{f<test>/r<NNN>/gis/} for inspection in QGIS.}

\item{folds}{Optional fold spec overriding \code{config$folds}: a list of \code{c(test, val)}
group pairs (or a data.frame with \code{test}/\code{val} columns). Defaults to the base
config's single \code{test_group}/\code{val_group}.}

\item{anchor}{If TRUE, also run the full-transect (r = Inf) anchor as an endpoint
("r = infinity"). Defaults to \code{config$anchor}.}

\item{pin_weights}{If TRUE, train every cell with class weights pinned to the fold's
full-transect frequency, removing radius-dependent loss weighting. Defaults to
\code{config$pin_class_weights}.}

\item{comment}{Optional slurmcollie comment.}
}
\description{
Launches the pixel-degradation experiment, which measures how U-Net accuracy
responds to field plot size (sampling footprint) at fixed resolution. Training
transects are carved to radius-\code{r} disks around fixed synthetic plot centers,
while validation and test labels stay at full extent. Runs in two stages: \code{prep}
carves and exports patches (one CPU job per radius); \code{train} fits and evaluates
one GPU job per radius x seed cell. Run \code{prep} first, then \code{train} once prep
completes. See \code{inst/dev/pixel_degradation_experiment.md}.
}
