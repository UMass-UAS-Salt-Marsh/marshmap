% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/do_layer.R
\name{apply_fills}
\alias{apply_fills}
\title{Recursively apply fill rules to a raster}
\usage{
apply_fills(r, rules, source_dir, source_id_rast, source_maps, allowmissing)
}
\arguments{
\item{r}{(SpatRaster) the current state of the integrated raster}

\item{rules}{(list) fill rules at this level - each entry has \code{code},
\code{map}, optionally \code{fill}}

\item{source_dir}{(character) directory containing source maps}

\item{source_id_rast}{(SpatRaster) parallel raster of source IDs}

\item{source_maps}{(character) vector of source map names, indexed by
the values in \code{source_id_rast}}

\item{allowmissing}{(logical) passed to recursion}
}
\value{
List with \code{raster}, \code{source_id}, and \code{source_maps}
}
\description{
For each rule, loads the fill map, recursively applies any nested fills
\emph{to the fill map} (so they're scoped to its territory), then merges the
processed fill map into the parent raster wherever the parent has the
rule's code. This scoping prevents a nested fill from accidentally
overwriting cells in the parent that happen to share a code with the
fill map.
}
\keyword{internal}
