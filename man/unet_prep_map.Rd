% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unet_prep_map.R
\name{unet_prep_map}
\alias{unet_prep_map}
\title{Prepare map patches for U-Net prediction}
\usage{
unet_prep_map(
  fitid = NULL,
  model = NULL,
  clip = NULL,
  resources = NULL,
  local = FALSE,
  trap = TRUE,
  comment = NULL
)
}
\arguments{
\item{fitid}{Fit id in the fits database. The model name is resolved from
the database. Provide either \code{fitid} or \code{model}, not both.}

\item{model}{The model name (base name of the prep \code{.yml} in \verb{<pars>/unet/}).
Use this to prep map patches for a model that may have multiple training
runs, or before any training has been registered in the fits database.}

\item{clip}{Optional clip extent, vector of \code{xmin}, \code{xmax}, \code{ymin}, \code{ymax}.
If NULL, tiles the full ortho extent.}

\item{resources}{Slurm launch resources. See \link[slurmcollie]{launch}.}

\item{local}{If TRUE, run locally; otherwise, spawn a batch run on Unity}

\item{trap}{If TRUE, trap errors in local mode}

\item{comment}{Optional slurmcollie comment}
}
\description{
Tiles the full ortho extent (or a clipped region) into overlapping patches
for wall-to-wall prediction. Patches are saved as numpy arrays alongside
a CSV of spatial origins for later reassembly.
}
