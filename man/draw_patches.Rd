% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw_patches.R
\name{draw_patches}
\alias{draw_patches}
\title{Draw training patches for visual QC}
\usage{
draw_patches(
  data_dir,
  site,
  dataset = "train",
  n = 5,
  patchno = NULL,
  rgb_channels = c(3, 2, 1),
  class_colors = NULL,
  output_dir = NULL,
  overlay_alpha = 0.5
)
}
\arguments{
\item{data_dir}{Directory containing numpy files (e.g., .../set1)}

\item{site}{Site name (e.g., 'NOR')}

\item{dataset}{Which dataset to draw from: 'train', 'validate', or 'test'}

\item{n}{Number of patches to draw (default 5). Ignored if patchno is specified.}

\item{patchno}{Optional integer vector of specific patch indices to draw}

\item{rgb_channels}{Which channels to use for RGB display (default c(3, 2, 1)
for Red, Green, Blue assuming MicaSense band order: Blue, Green, Red, RedEdge, NIR)}

\item{class_colors}{Named list mapping remapped class values to hex colors}

\item{output_dir}{Directory to write TIFFs (default: data_dir)}

\item{overlay_alpha}{Transparency for class overlay, 0-1 (default 0.5)}
}
\value{
Invisible vector of output file paths
}
\description{
Reads numpy patch files and writes RGB TIFFs with class labels
overlaid in color. Used to verify that the entire pipeline from shapefile
through patch extraction through numpy export produces correct, aligned data.
}
